[color]
    branch = auto
    diff = auto
    grep = auto
    interactive = auto
    status = auto

[core]
    autocrlf = false
    editor = code --wait
    excludesfile = ~/.gitignore.global

[include]
    path = .git_aliases
    path = .gitconfig.local
